---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ntfy_access Resource - ntfy"
subcategory: ""
description: |-
  Manages topic access permissions for an ntfy user.
---

# ntfy_access (Resource)

Manages topic access permissions for an ntfy user.

## Example Usage

```terraform
resource "ntfy_access" "example" {
  username   = "myuser"
  topic      = "alerts"
  permission = "read-write"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `permission` (String) The permission level: `read-write`, `read-only`, `write-only`, or `deny`.
- `topic` (String) The topic to grant access to. May end with `*` for wildcard matching.
- `username` (String) The username to grant access to. Use `*` or `everyone` for wildcard grants.

### Read-Only

- `id` (String) The unique identifier for this access grant (format: username/topic).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/usr/bin/env bash

terraform import ntfy_access.example myuser/alerts
```
